# build
bin/
obj/
fetcher.exe
*.dll

# data / generated
*.db
*.db-shm
*.db-wal
*.db.schema
jobs.raw.json
output/
fetcher/stderr.log

# secrets / local overrides
appsettings.local.json
.env

# saved CV-derived profile (personal data, machine-local)
profile.json
# LLM backend settings saved from the in-app Settings screen (machine-local)
llm-settings.json
# UI prefs (theme) saved from the Settings screen (machine-local)
ui-settings.json
# Apify connector settings — contains a secret API token (machine-local)
apify-settings.json
# JSearch connector settings — contains a secret RapidAPI key (machine-local)
jsearch-settings.json
# Keyless connector toggles (machine-local)
jobicy-settings.json
himalayas-settings.json
# saved career plan (built from the personal profile, machine-local)
career-plan.json
# growth history — archived past plans (machine-local)
career-plan-history.json
# resume cache — completed synthesis parts of an interrupted run (machine-local)
career-plan-parts.json
# persisted reasoning transcript for the saved plan (machine-local)
career-plan.reasoning.txt
# short-lived career-research cache (machine-local)
career-research.json
# per-company employer-health cache (machine-local)
company-reports.json
# per-company job-card briefing cache (machine-local)
company-briefs.json
# coach conversation history + pasted screenshots (personal data, machine-local)
coach-history.json
coach-images/
# CV Studio structured document + assistant chat (personal data, machine-local)
cv-data.json
cv-chat.json

# app / dev scratch
app-err.log
app-out.log
linkedin-jobs.json
score-schema.json
fetcher/stderr.log
schema-*.log
schema-test.json
